/** @type {import("jest").Config} **/
module.exports = {
  // When the projects configuration is provided with an array of paths or glob patterns,
  // Jest will run tests in all of the specified projects at the same time
  projects: ['packages/*'],
};